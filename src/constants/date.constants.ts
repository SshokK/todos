export enum DATE_GRANULARITY {
  DAY = 'day',
}

export enum DATE_FORMATS {
  API_DATE = 'yyyy-MM-dd',
  API_DATE_TIME = "yyyy-MM-dd'T'HH:mm:ss.SSS",
  API_DATE_TIME_WITH_Z = "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'",
}

export enum TIMEZONE {
  UTC = 'UTC',
}
