import * as utils from 'utils';

export const INITIAL_DATE = utils.getToday();

export const COLUMNS_COUNT = 5;

export const SIDE_COLUMNS_COUNT = Math.floor(COLUMNS_COUNT / 2);

export const PAGINATION_THROTTLE_TIME = 250;
